[project]
name = "langchain-getting-started"
version = "0.1.0"
description = "Chapter 1 — LangChain 1.0 in one hour"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.0,<2",
    "langchain-core>=1.0,<2",
    "langchain-ollama>=1.0,<2",
    "langchain-openai>=1.0,<2",
    "pydantic>=2.7,<3",
]

[tool.uv]
package = false
