[project]
name = "embedchain-test"
version = "0.1.0"
description = "RAG over PDFs with LlamaIndex (replaces embedchain)"
requires-python = ">=3.12"
dependencies = [
    "llama-index-core>=0.14,<0.15",
    "llama-index-llms-ollama>=0.10,<1",
    "llama-index-embeddings-huggingface>=0.7,<1",
]

[tool.uv]
package = false
