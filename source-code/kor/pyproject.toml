[project]
name = "kor"
version = "0.1.0"
description = "Date extraction with LangChain 1.0 structured output (replaces kor library)"
requires-python = ">=3.12"
dependencies = [
    "langchain-ollama>=1.0,<2",
    "langchain-core>=1.0,<2",
    "pydantic>=2.7,<3",
]

[tool.uv]
package = false
