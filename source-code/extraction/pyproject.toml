[project]
name = "extraction"
version = "0.1.0"
description = "Structured data extraction with LangChain 1.0"
requires-python = ">=3.12"
dependencies = [
    "langchain-ollama>=1.0,<2",
    "langchain-core>=1.0,<2",
    "pydantic>=2.7,<3",
]

[tool.uv]
package = false
