[project]
name = "langgraph-durable"
version = "0.1.0"
description = "Chapter 8 — Durable, restart-safe LangGraph agents"
requires-python = ">=3.12"
dependencies = [
    "langgraph>=1.0,<2",
    "langgraph-checkpoint-sqlite>=3.0,<4",
    "langchain-core>=1.0,<2",
    "langchain-ollama>=1.0,<2",
]

[tool.uv]
package = false
