[project]
name = "summarization"
version = "0.1.0"
description = "Text summarization with a local Ollama model"
requires-python = ">=3.12"
dependencies = [
    "langchain-ollama>=1.0,<2",
    "langchain-core>=1.0,<2",
]

[tool.uv]
package = false
