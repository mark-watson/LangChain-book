[project]
name = "sql-agent"
version = "0.1.0"
description = "Chapter 8 — Natural-language SQLite queries with a LangGraph SQL agent"
requires-python = ">=3.12"
dependencies = [
    "langgraph>=1.0,<2",
    "langchain-core>=1.0,<2",
    "langchain-ollama>=1.0,<2",
    "sqlalchemy>=2.0,<3",
]

[tool.uv]
package = false
