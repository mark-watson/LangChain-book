[project]
name = "cooking-recipes"
version = "0.1.0"
description = "Recipe generation with RAG and LangChain 1.0"
requires-python = ">=3.12"
dependencies = [
    "langchain-ollama>=1.0,<2",
    "langchain-chroma>=1.0,<2",
    "langchain-huggingface>=1.0,<2",
    "sentence-transformers>=3.0,<4",
]

[tool.uv]
package = false
