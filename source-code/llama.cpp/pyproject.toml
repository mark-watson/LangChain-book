[project]
name = "llama-cpp"
version = "0.1.0"
description = "Local GGUF model inference with LangChain 1.0"
requires-python = ">=3.12"
dependencies = [
    "langchain-community>=0.4,<1",
    "langchain-core>=1.0,<2",
    "llama-cpp-python>=0.3,<1",
]

[tool.uv]
package = false
