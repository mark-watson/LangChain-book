[project]
name = "langgraph-hitl"
version = "0.1.0"
description = "Chapter 6 — Human-in-the-loop patterns in LangGraph"
requires-python = ">=3.12"
dependencies = [
    "langgraph>=1.0,<2",
    "langchain-core>=1.0,<2",
    "langchain-ollama>=1.0,<2",
]

[tool.uv]
package = false
