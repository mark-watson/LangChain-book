[project]
name = "kg-agent"
version = "0.1.0"
description = "Chapter 9 — DBpedia and Wikidata as LangGraph agent tools"
requires-python = ">=3.12"
dependencies = [
    "langgraph>=1.0,<2",
    "langchain-core>=1.0,<2",
    "langchain-ollama>=1.0,<2",
    "sparqlwrapper>=2.0,<3",
]

[tool.uv]
package = false
