[project]
name = "llama-index-agent"
version = "0.1.0"
description = "Chapter 16 — Building an agent as a Workflow"
requires-python = ">=3.12"
dependencies = [
    "llama-index-core>=0.14,<0.15",
    "llama-index-llms-ollama>=0.10,<1",
    "ddgs>=1.0",
]

[tool.uv]
package = false
