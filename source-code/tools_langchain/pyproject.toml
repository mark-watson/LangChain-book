[project]
name = "tools-langchain"
version = "0.1.0"
description = "ReAct agent with custom tools using LangGraph 1.0"
requires-python = ">=3.12"
dependencies = [
    "langchain-ollama>=1.0,<2",
    "langchain-core>=1.0,<2",
    "langgraph>=1.0,<2",
]

[tool.uv]
package = false
